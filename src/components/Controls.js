import React from 'react';

const Controls = () => {
  return (
    <div>
      <button>Mover izquierda</button>
      <button>Mover derecha</button>
      <button>Rotar</button>
    </div>
  );
};

export default Controls;
